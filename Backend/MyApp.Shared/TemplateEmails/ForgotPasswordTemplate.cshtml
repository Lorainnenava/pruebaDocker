<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Restablecimiento de Contraseña</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f7fa;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 600px;
            margin: 40px auto;
            background: #ffffff;
            border-radius: 10px;
            padding: 30px;
            box-shadow: 0 6px 12px rgba(0,0,0,0.1);
        }

        .header {
            text-align: center;
            padding-bottom: 20px;
            border-bottom: 1px solid #eee;
        }

            .header h2 {
                color: #2a7df0;
                margin: 0;
                font-size: 22px;
            }

        .content {
            margin: 20px 0;
            font-size: 15px;
            color: #333;
            line-height: 1.6;
            text-align: center;
        }

        .code-box {
            text-align: center;
            margin: 25px 0;
        }

        .code {
            display: inline-block;
            font-size: 28px;
            font-weight: bold;
            color: #2a7df0;
            background: #f0f6ff;
            padding: 14px 28px;
            border-radius: 8px;
            letter-spacing: 4px;
        }

        .footer {
            margin-top: 30px;
            font-size: 12px;
            color: #777;
            text-align: center;
            border-top: 1px solid #eee;
            padding-top: 15px;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Encabezado -->
        <div class="header">
            <h2>Restablecimiento de Contraseña</h2>
        </div>

        <!-- Contenido -->
        <div class="content">
            <p>Hemos recibido una solicitud para restablecer su contraseña.</p>
            <p>Utilice el siguiente código para continuar con el proceso:</p>
        </div>

        <!-- Código de verificación -->
        <div class="code-box">
            <span class="code">{{CodeValidation}}</span>
        </div>

        <div class="content">
            <p>Este código es válido por un tiempo limitado.</p>
            <p>Si usted no solicitó restablecer su contraseña, puede ignorar este mensaje.</p>
        </div>

        <!-- Footer -->
        <div class="footer">
            <p>© 2025 Tu Aplicación. Todos los derechos reservados.</p>
        </div>
    </div>
</body>
</html>
